# create venv 
python -m venv venv
# linux / mac
source venv/bin/activate
# windows
# .\venv\Scripts\activate

pip install -r reqrequirements.txt


# run via uvicorn (recommended)
uvicorn backend:app --reload --host 127.0.0.1 --port 5000


